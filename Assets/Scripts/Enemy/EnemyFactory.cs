using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;

public class EnemyFactory : MonoBehaviour
{
    // // Start is called once before the first execution of Update after the MonoBehaviour is created
    // [SerializeField]
    // private GameObject enemyPrefab;
    // public List<EnemyData> enemyDataList;
    // private List<GameObject> objects;
    // public const int POOL_OBJECTS = 20;

    // void Start()
    // {
    //     for(int i = 0; i < POOL_OBJECTS; i++ ){
    //         var enemy = Instantiate(enemyPrefab);
    //         objects.Add(enemy);
    //         // enemy.GetComponent<EnemyBase>().Setup();
    //         enemy.SetActive(false);
    //     }
    // }

    // private GameObject GetNullEnemy(){
    //     for(int i = 0; i < POOL_OBJECTS; i ++){
    //         if(objects[i].activeSelf == true){
    //             return objects[i];
    //         }
    //     }
        
    // }

    // // Update is called once per frame
    // void Update()
    // {
        
    // }
    // void Spawan(EnemyData data)
    // {
    //     var enemy = GetNullEnemy();
    //     //
    // }
}
